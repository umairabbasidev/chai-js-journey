"use strict"; // treat all js code as newer version
// alert(3 + 3);

// In programming code readability should be high

// datatypes in js

// Primitive dataypes are 7:
// NN SS BB U

let age = 22; // 2 to power 53 nunber data types
let emptyVal = null; // null stand alone value
let symbol1 = Symbol("123"); // symbol datatype for uniqueness.
let symbol2 = Symbol("123");
// practical example console.log(symbol1 === symbol2); false.
let name = "Umair Abbasi"; // string data type
let isLooggedIn = true; // false:  // boolean yes ot no
let bigNumber = 12223334444444440n; // bigint datatype use for bignumber
let noDefinedVal = undefined; // declared vaiable but value is not defined


// Non primitive datatype
// object